/**
 * Placeholder — generación de PDFs (boletas, facturas, OCs, guías, fichas técnicas).
 * Para producción: usar `@react-pdf/renderer` en el backend o `pdfkit` en Edge Functions.
 */

export function pdfPlaceholder(): string {
  return 'PDF generation placeholder — integrar @react-pdf/renderer';
}
